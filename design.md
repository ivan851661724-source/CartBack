# CartBack 设计规则

## 一、阴影设计规则

### 1.1 阴影层级体系

| 层级 | 使用场景 | 值 |
| --- | --- | --- |
| 卡片阴影 | 所有 `.card` 容器、表格容器 | `0 2px 24px rgba(0,0,0,.06), 0 1px 4px rgba(0,0,0,.04)` |
| 顶栏阴影 | Topbar 底部 | `0 2px 16px rgba(0,0,0,.06)` |
| 头像阴影 | Av 组件 | `0 2px 8px rgba(0,0,0,.14), inset 0 1px 0 rgba(255,255,255,.15)` |
| 按钮阴影 | `.btn-primary` | `0 1px 4px rgba(255,143,88,.28)` |
| 海报模板 | 邮件配置海报选项 | `0 1px 4px rgba(0,0,0,.1)` |

### 1.2 阴影设计要点

- **卡片容器**：双层阴影叠加 — 大扩散环境光 `0 2px 24px rgba(0,0,0,.06)` + 小接触阴影 `0 1px 4px rgba(0,0,0,.04)`，适用于所有 `.card` 类和表格容器。
- **顶栏底部**：单层中等扩散 `0 2px 16px rgba(0,0,0,.06)`，与卡片同不透明度但扩散半径收窄，用于区分层级。
- **头像**：`0 2px 8px rgba(0,0,0,.14)` + 内嵌高光 `inset 0 1px 0 rgba(255,255,255,.15)`，不透明度更高以突出人像元素。
- **主色调按钮**：使用品牌色阴影 `0 1px 4px rgba(255,143,88,.28)`；其余小元素（如海报模板）使用 `0 1px 4px rgba(0,0,0,.1)`。

### 1.3 扩散半径规律

> 卡片 24px → 顶栏 16px → 头像 8px → 小元素 4px，**层级越小扩散越收**。

- 彩色阴影**仅限**主色调按钮使用。
- 阴影不透明度统一以 `.06` 为基底，仅在头像等需突出的元素上提升至 `.14`。

---

## 二、圆角设计规则

### 2.1 圆角层级体系

| 层级 | 使用场景 | 值 |
| --- | --- | --- |
| 页面级容器 | `.card` 类、表格外框、各功能区主容器 | `16px` |
| 交互组件 | 输入框、下拉选择、引导浮层容器、访客详情面板 | `10px` |
| 默认按钮 | `.btn` | `9px` |
| 小号按钮 | `.btn-sm` | `7px` |
| 浮层迷你按钮 | 引导浮层内 | `6px` |
| 标签/徽章 | `.tag`、`.chip`、`.status-*` | `999px`（全圆） |
| 头像 | Av 组件 | `50%`（正圆） |

### 2.2 圆角设计规律

> 容器从外到内圆角递减：`16 → 10 → 9/7/6`，**全圆仅用于行内徽章类元素**。

- 页面级卡片容器统一 `16px`，适用于所有 `.card` 类、表格外框、以及各功能区的主容器。
- 交互组件统一 `10px`，包括输入框、下拉选择、引导浮层容器、以及用户管理的访客详情面板。
- 按钮和标签使用 `9px`（`.btn` 默认），小号按钮收为 `7px`（`.btn-sm`），引导浮层内的迷你按钮进一步收为 `6px`。
- 标签 / Tag 元素和状态徽章使用 `999px` 全圆，适用于所有 `.tag`、`.chip`、`.status-*` 类。
- 头像始终为正圆（`50%`）。

---

## 三、色彩设计规则

### 3.1 品牌主色

| 色值 | 用途 |
| --- | --- |
| `#FF7F4D` | 主橙（按钮、强调、激活态） |
| `#FFB380` | 浅橙（渐变尾色、头像） |

### 3.2 背景层

| 色值 | 用途 |
| --- | --- |
| `#FCFDFF` | 全局页面底色 |
| `#FAFAF9` | 区块次级背景（section header） |
| `#F8F7F5` | 弹窗背景 |
| `#F3F1EE` | 表格 header、代码块背景（暖调残留） |
| `#F6F7F9` | 输入框、优惠码框背景 |
| `#EEF2F6` | 表行 hover 背景 |
| `#fff` / `#ffffff` | 卡片、侧边栏、顶栏白色 |

### 3.3 描边 / 分隔

| 色值 | 用途 |
| --- | --- |
| `#DDE2E8` | 全局统一描边色 |

### 3.4 文字

| 色值 | 用途 |
| --- | --- |
| `#1E293B` | 主文本（近黑） |
| `#8A95A0` | 辅助文本 / 侧边栏未选中（统一态） |
| `#A8B4BE` | 三级文本（占位符、更淡提示） |

### 3.5 状态色

| 色值 | 用途 |
| --- | --- |
| `#16A34A` / `#15803D` | 成功绿 |
| `#DCFCE7` / `#F0FDF4` | 绿色浅背景 |
| `#86EFAC` | 绿色描边 |
| `#D97706` / `#C05A1A` | 警告橙 |
| `#FCD34D` | 警告黄描边 |
| `#FEF3C7` | 黄色浅背景 |
| `#DC2626` | 错误红 |
| `#FEE2E2` | 红色浅背景 |

### 3.6 标签 / 装饰

| 色值 | 用途 |
| --- | --- |
| `#6366F1` / `#A78BFA` | 靛蓝（用户头像渐变、Variant B 海报） |
| `#3730A3` | 深靛（tag-default 文字） |
| `#E0E7FF` | 靛蓝浅背景（tag-default） |
| `#92400E` | 深琥珀（tag-price 文字） |
| `#D6CFC6` / `#B0A99F` | 灰米（Variant C 海报渐变） |

### 3.7 遗留暖调（可考虑清理）

| 色值 | 用途 | 备注 |
| --- | --- | --- |
| `#F3F1EE` | 表格 header 背景 | 与整体冷调有轻微偏差 |
| `#1a1a1a` / `#444` / `#888` | 邮件预览内模拟 email 客户端文字 | 可保留作刻意区分 |
