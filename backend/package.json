{
  "name": "cartback",
  "version": "3.0.0",
  "description": "CartBack v3 — IGDE 引导式对话驱动的邮件挽回增长助手（本地部署）",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "build": "tsc -p tsconfig.json",
    "pretest": "tsc -p tsconfig.json",
    "test": "node --test test/*.test.js",
    "selftest": "node dist/mailgen.js --selftest",
    "gallery": "node dist/gallery.js",
    "eval:offline": "node eval/runner.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "@napi-rs/canvas": "^0.1.65",
    "js-yaml": "^4.1.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.3"
  },
  "license": "MIT"
}
