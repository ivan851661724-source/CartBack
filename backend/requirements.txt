# CartBack v3 backend Python 邮件生成子系统依赖
# 安装：cd backend && python3 -m pip install -r requirements.txt
#
# 说明：
#   requests     — 调用 LLM / 万相 / Pollinations / 下载图片（必需）
#   pyyaml       — 解析 emailgen/emailgen_config.yaml（可选，无 YAML 时退化为环境变量/Node 注入）
#   pillow       — 图片后期叠加折扣/CTA（可选，缺失时 Pollinations 兜底不会叠加文字但仍能出图）
#
# 版本都取「>= 一个较老可用版本」，避免与系统/venv 冲突。
requests>=2.31.0
PyYAML>=6.0
Pillow>=10.0.0
